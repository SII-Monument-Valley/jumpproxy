# host	user	remote_port	local_port	ssh_config
# Use "-" for user or ssh_config to inherit from the selected defaults/config.
# Add optional bootstrap hosts below. Keep this file empty when connections
# should be registered after installation with `jumpproxy add HOST`.
